 #include<stdio.h>
 
void display (int arr[], int n)
 
   // Traversal 
    for (int i = 0; i < n; i++)
    {
        printf("%d" , arr[i]);

    printf("\n");
         

}
void intInsertion(int arr[],int size, int element,int capacity,int index){
   if(size>=capacity){
    return -1;
   }
     for (int i = size-1; i >=index; i++)
{
     arr[i+1] =arr[i;]
}
    a[index]= element;
     return


  int main(){
     int arr[100] ={1,2,6,78};
     int size = 5 element = 45, index=3;
     display (arr,size);
     intInsertion(arr,size,int,element,100 index);
      



     return 0;
  }
